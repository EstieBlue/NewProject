# This is the main file for the BFS implementation
# Where essentially the robots are created and the BFS algorithm is executed to find the shortest path in a graph.
# Without the multiple robots colliding with each other, the BFS algorithm will find the shortest path for each robot to reach its destination.